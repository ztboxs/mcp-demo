---
alwaysApply: true
---
# MCP Interactive Feedback 规则

注意在任务完成前根据 mcp-feedback-enhanced 工具的要求进行工具调用

## 默认配置

调用 `user-mcp-feedback-enhanced-interactive_feedback` 工具时，使用以下参数：

- **timeout: 3600** (1小时)
- 确保有足够时间收集用户反馈

## 超时处理策略

当工具调用返回 `❌ 操作超时` 时：

1. **自动重试**: 立即再次调用工具，保持相同的 summary 内容
2. **通知用户**: 在 summary 中添加 "⏰ 等待您的反馈中..." 提示
3. **最多重试 3 次**: 避免无限循环

## 调用规范

```
timeout: 3600  (必须设置，不要使用默认值)
```

## 注意事项

- MCP 协议不支持自动续期，只能通过设置较长的初始 timeout 来解决
- 如果用户长时间未响应导致超时，应该友好地继续等待或询问用户
